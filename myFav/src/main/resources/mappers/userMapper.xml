<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  
 <mapper namespace="com.peta.mapper.UserMapper">
<select id="userName" resultType="com.peta.domain.UserVO">
	select userid,username from user where userid = #{userid} and userpw = #{userpw} 	
</select>

 </mapper>